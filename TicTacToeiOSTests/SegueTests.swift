import Quick
import Nimble
import TicTacToeiOS

class SegueTests: QuickSpec {
    override func spec() {

        xit("correctly sets the game type in a human vs. human game") {}
        xit("correctly sets the game type in a human vs. computer game") {}

        xit("correctly sets the players in a human vs. human game") {}
        xit("correctly sets the players in a human vs. computer game") {}

    }
}
